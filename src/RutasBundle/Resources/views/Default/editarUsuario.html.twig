{% extends 'base.html.twig' %}
{% block body %}
  <div class="w3-card-4">
    <div class="w3-container w3-green">
      <h2>Editar un usuario</h2>
    </div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
  </div>

{% endblock %}
